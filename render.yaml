services:
  - type: web
    name: stock-price-prediction-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api.predict:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
